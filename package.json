{
  "name": "tile-estate-story",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run process:tiles && tsc && vite build",
    "preview": "vite preview",
    "process:tiles": "node scripts/process-tiles.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
